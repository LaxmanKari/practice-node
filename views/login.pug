extends layouts/login-layout.pug 

block content 
    .loginContainer 
        h1 Login 
        p.errorMessage #{errorMessage}
        form(method="post", id="login-form") 
            input(type="text", name="logUsername", placeholder="Username or email", value = "laxman",required="")
            input(type="password", name="logPassword", placeholder="Password", value=1,required="")
            input(type="submit", value="Login")
        a(href="/register") Need an account? Register here. 
    
    